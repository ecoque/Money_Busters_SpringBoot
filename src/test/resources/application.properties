# --- TEST ORTAMI AYARLARI ---

# Test s?ras?nda gerçek environment variable'lar olmad??? için
# placeholder'lar? (de?i?kenleri) burada elle tan?ml?yoruz.

# Buraya test için geçerli bir kullan?c? (UPT veya DENEME_USER) yaz.
# Testler bu kullan?c? ile veritaban?na ba?lan?p kontrol yapacak.
DB_URL=jdbc:oracle:thin:@localhost:1521/XEPDB1
DB_USER=UPT
DB_PASSWORD=upt123

# Spring'e bu de?i?kenleri kullanmas?n? söyleyelim (Opsiyonel ama garanti olsun)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver


spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=none